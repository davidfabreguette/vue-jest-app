import hello_vue from "../app.vue"

describe("hello-vue", () => {

  describe("chocolate", () => {
    it("has a chocolate method", () => {
      expect("this").toBe("this")
      // const vm = new Vue(about).$mount()
      // expect(vm.chocolate()).toBe("good");
    })
  })
})
